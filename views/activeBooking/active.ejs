<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Active Bookings</title>
    <link rel="stylesheet" type="text/css" href="/css/active.css" />
  </head>
  <body>
    <nav>
      <form action="/activeBooking" method="POST">
        <button type="submit" class="nav-item active">Active booking</button>
      </form>
      <form action="/confirmPayment" method="POST">
        <button type="submit" class="nav-item">Confirm of payment</button>
      </form>
      <form action="/cancelRequests" method="POST">
        <button type="submit" class="nav-item">Cancel requests</button>
      </form>
    </nav>
    <div class="container">
      <h1>Активные (подтверждённые) бронирования</h1>
      <table>
        <thead>
          <tr>
            <th>#</th>
            <th>Клиент</th>
            <th>Телефон</th>
            <th>Дата</th>
            <th>Номер</th>
            <th>Ночей</th>
            <th>Стоимость</th>
          </tr>
        </thead>
        <tbody>
          <% let count = 1; %> <% if (client && client.length > 0) { %> <%
          client.forEach(function(book) { %>
          <tr>
            <td><%= count %></td>
            <td><%= book.fullName %></td>
            <td><%= book.phoneNumber %></td>
            <td>
              <%= book.date ? (() => {
                const [day, month, year] = book.date.split(':').map(Number);
                return new Date(year, month - 1, day).toLocaleDateString('uk-UA', {day: '2-digit', month: '2-digit', year: 'numeric'});
              })() : '' %>
            </td>
            <td><%= book.classRoom %></td>
            <td><%= book.night %></td>
            <td><%= book.price %> грн</td>
          </tr>
          <% count++; %> <% }); %> <% } else { %>
          <tr>
            <td colspan="7">Нет активных подтверждённых броней</td>
          </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </body>
</html>
