<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirm payment</title>
    <link rel="stylesheet" href="style.css">
  </head>
  <body>
    <div class="container">
      <h1>Подтверждение бронирования</h1>
      
      <div class="bookings-list">
        <% if (booking && booking.length > 0) { %>
          <% booking.forEach(book => { %>
            <div class="booking-card">
              <div class="booking-info">
                <h3>Бронь #<%= book._id %></h3>
                <p><strong>Клиент:</strong> <%= book.fullName %></p>
                <p><strong>Телефон:</strong> <%= book.phoneNumber %></p>
                <p><strong>Дата:</strong> <%= new Date(book.date).toLocaleDateString() %></p>
                <p><strong>Время:</strong> <%= book.time %></p>
                <p><strong>Номер:</strong> <%= book.classRoom %></p>
                <p><strong>Количество ночей:</strong> <%= book.night %></p>
                <p><strong>Стоимость:</strong> <%= book.price %> грн</p>
              </div>
              
              <div class="booking-actions">
                <form action="/confirmBooking" method="POST">
                  <input type="hidden" name="userName" value="<%= book.userName %>">
                  <input type="hidden" name="date" value="<%= book.date %>">
                  <input type="hidden" name="time" value="<%= book.time %>">
                  <input type="hidden" name="beforeDate" value="<%= book.beforeDate %>">
                  <input type="hidden" name="classRoom" value="<%= book.classRoom %>">
                  <input type="hidden" name="night" value="<%= book.night %>">
                  <input type="hidden" name="price" value="<%= book.price %>">
                  <input type="hidden" name="phoneNumber" value="<%= book.phoneNumber %>">
                  <input type="hidden" name="fullName" value="<%= book.fullName %>">
                  <button type="submit" class="btn confirm">Подтвердить бронь</button>
                </form>
                
                <form action="/declineBooking" method="POST" class="decline-form">
                  <input type="hidden" name="userName" value="<%= book.userName %>">
                  <input type="hidden" name="fullName" value="<%= book.fullName %>">
                  <input type="hidden" name="classRoom" value="<%= book.classRoom %>">
                  <input type="text" name="adminComment" placeholder="Причина отказа" required>
                  <button type="submit" class="btn decline">Отклонить бронь</button>
                </form>
              </div>
            </div>
          <% }); %>
        <% } else { %>
          <p class="no-bookings">Нет активных заявок на бронирование</p>
        <% } %>
      </div>

      <div class="navigation">
        <a href="/home" class="btn back">Вернуться на главную</a>
        <a href="/cancelRequests" class="btn cancel-requests">Заявки на отмену</a>
      </div>
    </div>
  </body>
</html>
