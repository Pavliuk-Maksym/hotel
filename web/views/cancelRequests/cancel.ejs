<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Заявки на отмену</title>
    <link rel="stylesheet" href="cancelRequest.css" />
  </head>
  <body>
    <nav>
      <form action="/activeBooking" method="POST">
        <button type="submit" class="nav-item">Active booking</button>
      </form>
      <form action="/confirmPayment" method="POST">
        <button type="submit" class="nav-item">Confirm of payment</button>
      </form>
      <form action="/cancelRequests" method="POST">
        <button type="submit" class="nav-item active">Cancel requests</button>
      </form>
    </nav>
    <div class="container">
      <h1>Заявки на отмену бронирования</h1>
      <table>
        <thead>
          <tr>
            <th>#</th>
            <th>Клиент</th>
            <th>Телефон</th>
            <th>Дата</th>
            <th>Номер</th>
            <th>Стоимость</th>
            <th>Возврат</th>
            <th>Действия</th>
          </tr>
        </thead>
        <tbody>
          <% let count = 1; %> <% if (requests && requests.length > 0) { %> <%
          requests.forEach(function(req) { %>
          <tr>
            <td><%= count %></td>
            <td><%= req.fullName %></td>
            <td><%= req.phoneNumber %></td>
            <td>
              <%= req.date ? new Date(req.date).toLocaleDateString() : '' %>
            </td>
            <td><%= req.classRoom %></td>
            <td><%= req.price %> грн</td>
            <td><%= req.refundAmount %> грн (<%= req.refundPercentage %>%)</td>
            <td>
              <form
                action="/confirmCancel"
                method="POST"
                style="display: inline-block"
              >
                <input type="hidden" name="requestId" value="<%= req._id %>" />
                <button type="submit">Подтвердить</button>
              </form>
              <form
                action="/declineCancel"
                method="POST"
                style="display: inline-block; margin-left: 5px"
              >
                <input type="hidden" name="requestId" value="<%= req._id %>" />
                <input
                  type="text"
                  name="adminComment"
                  placeholder="Причина отказа"
                  required
                  style="width: 110px"
                />
                <button type="submit">Отклонить</button>
              </form>
            </td>
          </tr>
          <% count++; %> <% }); %> <% } else { %>
          <tr>
            <td colspan="8">Нет активных заявок на отмену</td>
          </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </body>
</html>
